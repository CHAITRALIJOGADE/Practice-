Print Even Index Array Elements Description Take the size of an array and its elements as input from the user and print the elements of the array that are located at even indices.



import java.util.Scanner;

public class PrintEvenIndexElements {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int[] arr = new int[n];

        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }

        for (int i = 0; i < n; i += 2) {
            System.out.print(arr[i] + " ");
        }

        sc.close();
    }
}
